import styled from '@emotion/styled';

const StarHeaderContainer = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;

  h1 {
    font-size: 1.3rem;
  }
`;

export default StarHeaderContainer;
